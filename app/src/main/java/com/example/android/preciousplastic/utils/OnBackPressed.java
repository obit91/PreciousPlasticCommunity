package com.example.android.preciousplastic.utils;

public interface OnBackPressed {

    /**
     * This method should return true if it overrides the activity version.
     * @return true in case it overrides.
     */
    boolean onBackPressed();
}